apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: proxus-db-volume
  name: proxus-db-volume
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
